//Estrutura de repeticao (for)

//for (inicio; condicao; incremento) {
//  codigo a ser executado
//}
for (let index =0; index < index.length; index +=1)
console.log(index);

//for e array
let nomes = ['Eduardo', 'Maria', 'Joana', 'Wallace', 'Rosana'];
for (let index = 0; index < nomes.length; index +=1) {
    let mensagem = "Boas vindas, " + nomes[index] + "!";
    console.log(mensagem);
}