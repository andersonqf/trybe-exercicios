                                            **AULA 01**

getElementById - seleciona um elemento com a Id;
getElementsByClassName - lembrar que ele acessa sempre uma array, seliciona todos os elementos que tem uma classe;
getElementsBytagName - seleciona um elemento usando uma Tag, lembrar que ele acessa uma array ou objeto, por isso tem q informar a posicao [0] ou [1];
> Ex.: let (define-nome-da-variavel) = document.getElementById('nome-da-Id-no-HTML') 


querySelector - seleciona o preiro elemento que for localizado de cima para baixo da esquerda para direita
querySelectorAll - seleciona todos os elementos solicitado
>Ex.: let (define-nome-da-variavel) = querySelectorAll('#Id)   - Pode capturar o filho da tag ('.nome-Class nome-Filho')
Obs.: Usar # para capturar Id e '.' para capturar class


Para alterar o texto usamos o:
> innerText = muda o texto capturado pelo getElementById ou querySelector ou ....
> innerHTML = muda o texto e tambem as Tag's 


Enquanto os getElementsByClassName('ClassName') e getElementsByTagName('TagName') retornam uma HTMLCollection, os querySelectorAll('.ClassName') e querySelectorAll('TagName') retornam uma NodeList.
Obs. quando inspecionar a pagina dentro da aba console
> Html colection - so contem  elementos HTML
> NodeList - tem os elementos Html e textos

                                **AULA 02* TRABALHANDO COM ELEMENTOS **

A propriedade parentNode acessa o elemento pai do elemento sobre o qual a propriedade é chamada. Mas ela não é a única forma de, a partir de um elemento, navegar para outros.

Há, ao todo, as seguintes propriedades:

> parentNode: retorna o nó pai.
> parentElement: retorna o elemento pai.
> childNodes: retorna um NodeList com todos os nós filhos.
> children: retorna um HTMLCollection com todos os elementos filhos.
> firstChild: retorna o primeiro nó filho.
> firstElementChild: retorna o primeiro elemento filho.
> lastChild: retorna o último nó filho.
> lastElementChild: retorna o último elemento filho.
> nextSibling: retorna o próximo nó.
> nextElementSibling: retorna o próximo elemento.
> previousSibling: retorna o nó anterior.
> previousElementSibling: retorna o elemento anterior.

 _________________________________________________________________________________________________

 Criando elementos com:
 > createElement = cria um nó de elemento 'tag' Ex. (h1), (p), (li), ...
 > appendChild = adiciona o nó de texto criado com o comando createElement; 

 Removendo elementos com:
 >removeChild = 

 ************************************RESUMAO ATE AGORA****************************************
 É importante conhecer os comandos para criar, buscar e remover elementos, porem a dica é:
 >  segura na mao de 1 deles e segue a vida forever

 Qual a ordem pra seguir quando for adicionar algum elemento em um HTML pelo JS:
 > 1 - Capturo o elemento pai com o comando:
    let weekDays = document.getElementById('weekDays');

    let weekDaysList = ['Domingo', 'Segunda', 'Terca', 'Quarta', 'Quinta', 'Sexta', 'Sabado'];

> 2 - Crio o elemento:
    let weekday = document.creatElement ('li') ou ('p') ou ('div') ;

> 3 - Crio um valor pra esse elemento:
    weekDay.innetHTML = weekDayList[index];

> 4 - Adiciono o elemento:
    weekDays.appendChild(weekDay);

*****************************************************************************************************

                                    AULA 3 - EVENTOS

 